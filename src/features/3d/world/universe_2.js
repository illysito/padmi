import { World_2 } from './World_2' // cambiar la ruta si fuera necesario

function world_2(container) {
  // 1. Create an instance of the World app
  const world = new World_2(container)
  // 2. Render the scene
  world.start()
}

export default world_2
